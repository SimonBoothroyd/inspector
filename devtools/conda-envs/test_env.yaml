name: test
channels:
  - conda-forge
  - omnia
  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Core dependencies
  - click
  - mdtraj
  - openforcefield
  - pydantic

    # - Backend dependencies
  - fastapi
  - uvicorn

    # Test dependencies
  - pytest
  - pytest-cov
  - codecov
  - deepdiff

    # Developer dependencies
    # - Linting
  - isort
  - black
  - flake8