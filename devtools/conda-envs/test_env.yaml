name: test
channels:
  - conda-forge
  - omnia
  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Core dependencies
  - click
  - mdtraj
  - openforcefield
  - pydantic

    # Backend dependencies
  - fastapi

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - deepdiff

    # Linting
  - isort
  - black
  - flake8